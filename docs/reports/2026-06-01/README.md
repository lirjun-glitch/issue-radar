# 2026-06-01 Issue Radar

이 폴더는 날짜별 이슈 레이더 웹페이지를 보관한다.

현재 GitHub 커넥터의 텍스트 파일 생성 API로는 PDF/DOCX 바이너리 업로드가 제한되어 있어, 웹페이지 구조와 날짜별 HTML/JSON/CSS/JS부터 PR로 올린다. PDF/DOCX 파일은 PR 병합 후 GitHub 웹 UI에서 같은 폴더에 업로드하거나, 바이너리 업로드 권한이 열리면 자동화에 포함한다.
